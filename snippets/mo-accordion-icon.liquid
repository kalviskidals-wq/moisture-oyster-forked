{%- # CUSTOM: net-new snippet — the +/× expand toggle glyph for MO Ingredient Transparency's accordion rows (snippets/mo-ingredient-item.liquid). Rendered as a single plus-sign SVG; CSS rotates it 45deg when the parent <details> is [open], which turns a "+" into a "×" with no separate icon/JS needed (see .mo-ingredient__toggle rules in custom.css). Uses currentColor so the section's tab/accent color token can tint it. UPSTREAM risk LOW — no Horizon file touched. -%}
<svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true" focusable="false">
  <path d="M12 4V20" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
  <path d="M4 12H20" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
</svg>
